<?php

namespace {{ class.className.namespace }};

use {{ class.dataTransferObjectClassName.forUseStatement }};
use {{ class.entityClassName.forUseStatement }};

final class {{ class.className.name }} extends {{ class.dataTransferObjectClassName.name }}
{
    /**
     * @param {{ class.entityClassName.name }} ${{ class.entityClassName.forParameter }}
     */
    public function __construct({{ class.entityClassName.name }} ${{ class.entityClassName.forParameter }})
    {
        // make sure we have an existing {{ class.entityClassName.forParameter }}
        parent::__construct(${{ class.entityClassName.forParameter }});
    }
}
