<?php

namespace {{ class.className.namespace }};

use Doctrine\DBAL\Platforms\AbstractPlatform;
use Doctrine\DBAL\Types\Type;

final class {{ class.className.name }} extends Type
{
    /**
     * @return string
     */
    public function getName()
    {
        return '{{ class.name }}';
    }

    /**
     * @param {{ class.type }} ${{ class.className.forParameter }}
     * @param AbstractPlatform $platform
     *
     * @return {{ class.valueObjectClassName.name }}
     */
    public function convertToPHPValue(${{ class.className.forParameter }}, AbstractPlatform $platform)
    {
        return new {{ class.valueObjectClassName.name }}(${{ class.className.forParameter }});
    }

    /**
     * @param {{ class.valueObjectClassName.name }} ${{ class.className.forParameter }}
     * @param AbstractPlatform $platform
     *
     * @return string
     */
    public function convertToDatabaseValue(${{ class.className.forParameter }}, AbstractPlatform $platform)
    {
        return ${{ class.className.forParameter }}->getValue();
    }
}
