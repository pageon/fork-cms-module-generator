
  {{ service.className }}:
{% if service.tags|length > 0 %}
    tags:
{% for tag in service.tags %}
      - { {% for key, value in tag -%}{{ key }}: {{ value }}{% if not loop.last %}, {% endif %}{% endfor %} }
{% endfor %}
{% endif %}
