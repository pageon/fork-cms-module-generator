<?php

namespace {{ class.className.namespace }};

use Common\Doctrine\Type\AbstractFileType;

class {{ class.className.name }} extends AbstractFileType
{
    protected function createFromString(string $fileName): {{ class.fileClassName.name }}
    {
        return {{ class.fileClassName.name }}::fromString($fileName);
    }

    public function getName(): string
    {
        return '{{ class.name }}';
    }
}
